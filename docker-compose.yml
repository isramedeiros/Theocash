services:
  mysqldb:
    image: mysql:latest
    container_name: theocash-db
    restart: always
    environment:
      - MYSQL_DATABASE=Theocash
      - MYSQL_ROOT_PASSWORD=Got#9900
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: